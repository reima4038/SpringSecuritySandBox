server.port: 8030
spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:test
    username: test
    password: test
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate.ddl-auto: create-drop

logging:
  level:
    org:
      springframework:
        security: DEBUG